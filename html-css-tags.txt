HTML Basic Tags:
header, main, footer, aside, article, section, div, p, cite, em, i, b, u, strong, s, del, pre, sub, sup, code, tt, q, abbr, a, link, marquee and etc

HTML Form Tags and input tyeps:
number, text, password, email, tel, date-time, date-time-local, checkbox, select, option, radio btn, btn, submit, reset, month, year, week, color, file, hidden, range, search, url and etc

HTML Table Tags:
table, tr, td, caption, th, thead, tbody, tfooter, colspan, rowspan and etc


CSS Basic Style:
background, background-color, color(text color), font-size, font-style, font-weight, font-famliy, background-size, backgorund-position, padding, margin, position, display, text-align, background-repeat, border, border-radius, height, width, box-shadow

CSS Advance Style:
keyframe(animation), media quary (max-width, min-width), transform, transition, align-items, justify-content, gap, flex-direction, filter, text-indent, z-index, white-space, overflow, Pseudo-class, Pesudo-element, Ocpacity


- CSS Box Model
- CSS Grid View
- CSS list
- Card Creation
- Create a Dropdown Menu (without HTML Form - only CSS)
- Create Website Layout and skeleton
- CSS Loader
- CSS Pagination
- CSS Flexbox
- CSS Container


https://www.w3schools.com/css/css_exercises.asp

-------------------------------  CSS TASK  --------------------------

create a single html file that show below css class withn heading
1. create a divs with top, right, left, bottom borders with border style, border width, border color 
(ex. first div show only top border with 1px width, solid and black color)
 
 
 
2. create a div with pseudo class hover 
(ex. when you hover the div that change that color, size, position etc)
 
 
3. create a anchor tag that show pseudo class like :acitve and :visited 
(ex. normal anchor tag vs pseudo anchor tag)
 
 
4. create a section with different html tag and that show pesudo element like first-letter, first-line, before, after, selection 
(ex. in p tag when you select the text that show different color of background like red color background and white color text)
 
 
5. Show different types of display like none, block, inline, inline-block, flex, grid
(ex. h1 show inline display vs h1 without inline display (without style))
 
 
6. create a section that show position property like absolute, fixed, sticky, relative (ex. p tag that show fixed position property)
 
 
7. create div that show a image when user hover that div image will be zoom-in or zoom-out (overflow: visible, auto, hidden)
 
 
8. show html list with different type of bullets with css {list-style-type, list-style-position, list-style-image, }
(ex. unordered list with square bullets)
 
 
9. create a section that show css visibility property like hidden and visible
(ex. section with three image when user hover on images that hide and show)
 
 
10. create a section that show different type of cursors like pointer, auto, all-scroll, copy, default, move, e-resize, w-resize, n-resize, s-resize, text, wait, help, progress, not-allowed, zoom-in, zoom-out, no-drop (Min. 8 types)
(ex. create a div that show cursor pointer when user hover on it)
 
 
11. create a btn with div and css
 
 
12. show all css fitlers on image {filters like brightness, blur, Sepia, Grayscale, Hue-rotate, Contrast, Drop-shadow, Saturate} (don't change that image, same image show diffrent filters)
(ex. image that blur effect )
 
 
13. create a divs that show different color with different color syntax like hex, rgb, rgba, hsl, hsla, color name
 
 
14. All CSS Selectors like element selector, class selector, group selecors, universal selector, id selector
 
 
 
 
15. create a div that show box-shadow and h1 text that show text-shadow
 
 
16. create a two div one is show radial gradient and another is show linear gradient
 
 
17. write a text that show different types of text property like font-size, font-family, font-variant, font-color, font-style, font-weight 
(ex. first h1 show pain text, second show bigger font size, third show different font-family with font-size)
 
 
18. use three libraries like google font, font-awesome, bootstrap etc. (don't use svg)

------------------------------- Responsive, Animation And Advance CSS: ----------------------

1. Create a div that rotate when user hover on div
 
2. show difference flex vs grid display
 
3. create a section. In Section Show all justify-content property like center, flex-end, flex-start, space-between, space-around, space-evenly
 
4. create a paragraph that text-stroke property
 
5. Show use Of CSS Columns like Auto, Column-width, Column-count
 
 
6. Show use Of CSS Hyphens like none, Manual and Auto
 
7. Show Animation Using Transform-Origin
 
8. Show All CSS Transforms Property like translate, rotate, scale, skewX, skewY, skew, Metrix
 
9. Show 3D Rotation using Transforms And Animation (keyframes)
 
10. Show Use of CSS Transitions Property like transition, transition-delay, transition-duration, transition-property, transition-timing-function
 
11. create A animated Div that rotate and swift place in window
 
12. show use case of z-index
 
13. set text size with different units like px, em, rem, mm, in, pt, cm
 
14. create a product section with responsive design fit to desktop, tablet and mobile